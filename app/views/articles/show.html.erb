<div>
  <h1><%= @article.title %></h1>
</div>

<div>
  <p><%= @article.content %></p>
  <p><%= link_to 'Edit', edit_article_path(@article) %></p>
  <p><%= link_to 'Delete', article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %></p>
</div>

<div>
  <p><%= link_to 'Back to the article index', articles_path %></p>
</div>
